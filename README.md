Simple Json type for swift
==========================
Used as a replacement for `[String:Any]` (implemented as an `enum`)

- Restricted to Json types
- `Codable`,`Sendable`,`Hashable`,`Equatable`


`JsonView`
-------------------
- Simple SwiftUI `View` to view a `Json` as a tree